////////////////////////////////////////////////////////////////////////////
//                                                                        //
//             This file was generated with "slingr-helpgen"              //
//                                                                        //
//               For more info, check the following links:                //
//             https://www.npmjs.com/package/slingr-helpgen               //
//           https://github.com/slingr-stack/slingr-helpgen               //
//                                                                        //
////////////////////////////////////////////////////////////////////////////

endpoint.aiData = {};

endpoint.aiData.graphql = {};

endpoint.aiDataAdmin = {};

endpoint.aiDataAdmin.applications = {};

endpoint.aiDataAdmin.applications.eventEndpoint = {};

endpoint.aiDataAdmin.applications.submit = {};

endpoint.aiDataAdmin.applications.approve = {};

endpoint.aiDataAdmin.applications.reject = {};

endpoint.aiDataAdmin.applications.deploy = {};

endpoint.aiDataAdmin.applications.disable = {};

endpoint.aiDataAdmin.applications.enable = {};

endpoint.aiDataAdmin.organizations = {};

endpoint.aiDataAdmin.organizations.applications = {};

endpoint.aiDataAdmin.externalCredentials = {};

endpoint.aiDataAdmin.integration = {};

endpoint.aiDataAdmin.integration.org = {};

endpoint.aiDataAdmin.currentUser = {};

endpoint.aiDataAdmin.users = {};

endpoint.aiDataAdmin.users.me = {};

endpoint.aiDataAdmin.users.me.applications = {};

endpoint.aiDataAdmin.users.me.refreshUserCache = {};

endpoint.aiDataAdmin.signup = {};

endpoint.aiDataAdmin.signup.assignVertical = {};

endpoint.aiDataAdmin.signup.assignVertical.cloneProgress = {};

endpoint.aiDataAdmin.login = {};

endpoint.aiDataAdmin.signup.initial = {};

endpoint.aiDataAdmin.token = {};

endpoint.aiDataAdmin.token.logout = {};

endpoint.aiDataAdmin.token.refresh = {};

endpoint.aiDataAdmin.token.extend = {};

endpoint.aiDataAdmin.password = {};

endpoint.aiDataAdmin.password.requestReset = {};

endpoint.aiDataAdmin.password.reset = {};

endpoint.aiDataAdmin.passwordToken = {};

endpoint.aiDataAdmin.mfa = {};

endpoint.aiDataAdmin.mfa.token = {};

endpoint.aiDataAdmin.mfa.verify = {};

endpoint.aiDataAdmin.bearer = {};

endpoint.aiDataAdmin.orgInvite = {};

endpoint.aiDataAdmin.orgInvite.password = {};

endpoint.aiDataAdmin.orgInvite.password.reset = {};

endpoint.aiDataAdmin.users.reload = {};

endpoint.aiDataAdmin.openid = {};

endpoint.aiDataAdmin.openid.linkUserToOid = {};

endpoint.aiDataAdmin.organization = {};

endpoint.aiDataAdmin.organization.integration = {};

endpoint.aiDataAdmin.groups = {};

endpoint.aiDataAdmin.groups.application = {};

endpoint.aiDataAdmin.groups.application.organization = {};

endpoint.aiDataAdmin.organizations.openidRoles = {};

endpoint.aiDataAdmin.organizations.integration = {};

endpoint.aiDataAdmin.organizations.roles = {};

endpoint.aiDataAdmin.organizations.integrations = {};

endpoint.aiDataAdmin.organizations.paymentToken = {};

endpoint.aiDataAdmin.organizations.invoices = {};

endpoint.aiDataAdmin.openid.user = {};

endpoint.aiDataAdmin.openid.user.connection = {};

endpoint.aiDataAdmin.permissions = {};

endpoint.aiDataAdmin.analytics = {};

endpoint.aiDataAdmin.analytics.qlik = {};

endpoint.aiDataAdmin.analytics.qlik.app = {};

endpoint.aiDataAdmin.analytics.qlik.app.sheet = {};

endpoint.aiDataAdmin.analytics.qlik.organization = {};

endpoint.aiDataAdmin.analytics.qlik.organization.init = {};

endpoint.aiDataAdmin.analytics.qlik.stream = {};

endpoint.aiDataAdmin.tokens = {};

endpoint.aiDataAdmin.tokens.organizations = {};

endpoint.aiDataAdmin.tokens.organizations.tokens = {};

endpoint.aiDataAdmin.tokens.organizations.tokens.all = {};

endpoint.aiDataAdmin.users.mfa = {};

endpoint.aiDataAdmin.users.mfa.register = {};

endpoint.aiDataAdmin.users.mfa.register.verify = {};

endpoint.aiDataAdmin.users.mfa.default = {};

endpoint.aiDataAdmin.users.mfa.unregister = {};

endpoint.aiDataAdmin.roles = {};

endpoint.aiDataAdmin.roles.permissions = {};

endpoint.aiDataAdmin.signup.resend = {};

endpoint.aiDataAdmin.signup.verify = {};

endpoint.aiDataAdmin.users.permissions = {};

endpoint.aiDataAdmin.users.roles = {};

endpoint.aiDataAdmin.users.forcePasswordReset = {};

endpoint.aiDataAdmin.users.scimConnectIds = {};

endpoint.aiDataAdmin.users.me.userSettings = {};

endpoint.aiDataAdmin.users.me.acceptTerms = {};

endpoint.aiDataAdmin.users.me.acceptDevTerms = {};

endpoint.aiDataAdmin.users.me.changePassword = {};

endpoint.aiDataAdmin.users.me.setDeveloperOrganizationFields = {};

endpoint.aiDataAdmin.users.me.mfa = {};

endpoint.aiDataAdmin.users.me.mfa.register = {};

endpoint.aiDataAdmin.users.me.mfa.register.verify = {};

endpoint.aiDataAdmin.users.me.mfa.unregister = {};

endpoint.aiDataAdmin.username = {};

endpoint.aiDataAdmin.username.available = {};

endpoint.voice = {};

endpoint.voice.project = {};

endpoint.voice.projects = {};

endpoint.voice.project.clips = {};

endpoint.voice.project.tts = {};

endpoint.voice.project.tts.clip = {};

endpoint.voice.project.tts.clip.upload = {};

endpoint.voice.project.sts = {};

endpoint.voice.project.sts.clip = {};

endpoint.voice.sts = {};

endpoint.voice.sts.voices = {};

endpoint.voice.tts = {};

endpoint.voice.tts.preview = {};

endpoint.voice.project.tts.clip.convert = {};

endpoint.voice.project.sts.clip.convert = {};

endpoint.voice.lexicon = {};

endpoint.voice.lexicon.lexeme = {};

endpoint.voice.lexicon.lexeme.preview = {};

endpoint.voice.tts.voice = {};

endpoint.voice.tts.voice.versions = {};

endpoint.processing = {};

endpoint.processing.admin = {};

endpoint.processing.admin.resource = {};

endpoint.processing.admin.resource.engine = {};

endpoint.processing.admin.resource.service = {};

endpoint.processing.admin.serverType = {};

endpoint.processing.admin.serverType.add = {};

endpoint.processing.admin.terminate = {};

endpoint.processing.admin.licenses = {};

endpoint.processing.admin.licenses.apply = {};

endpoint.processing.admin.auth = {};

endpoint.processing.admin.backlog = {};

endpoint.processing.admin.backlog.clear = {};

endpoint.processing.admin.applications = {};

endpoint.processing.admin.applications.create = {};

endpoint.processing.admin.cores = {};

endpoint.processing.admin.cores.create = {};

endpoint.processing.admin.organizations = {};

endpoint.processing.admin.organizations.create = {};

endpoint.processing.admin.users = {};

endpoint.processing.admin.users.permission = {};

endpoint.processing.admin.users.permission.create = {};

endpoint.processing.admin.resource.create = {};

endpoint.processing.admin.users.role = {};

endpoint.processing.admin.users.role.create = {};

endpoint.processing.admin.serverTypes = {};

endpoint.processing.admin.serverTypes.create = {};

endpoint.processing.admin.serverTypes.runningEngine = {};

endpoint.processing.admin.serverTypes.runningEngine.create = {};

endpoint.processing.admin.service = {};

endpoint.processing.admin.service.create = {};

endpoint.processing.admin.tokens = {};

endpoint.processing.admin.tokens.create = {};

endpoint.processing.admin.users.create = {};

endpoint.processing.admin.applications.delete = {};

endpoint.processing.admin.licenses.delete = {};

endpoint.processing.admin.organization = {};

endpoint.processing.admin.organization.delete = {};

endpoint.processing.admin.resource.delete = {};

endpoint.processing.admin.users.role.delete = {};

endpoint.processing.admin.serverType.delete = {};

endpoint.processing.admin.serverType.engine = {};

endpoint.processing.admin.serverType.engine.delete = {};

endpoint.processing.admin.services = {};

endpoint.processing.admin.services.delete = {};

endpoint.processing.admin.token = {};

endpoint.processing.admin.token.delete = {};

endpoint.processing.admin.user = {};

endpoint.processing.admin.user.delete = {};

endpoint.processing.admin.serverType.desired = {};

endpoint.processing.admin.application = {};

endpoint.processing.admin.application.detail = {};

endpoint.processing.admin.core = {};

endpoint.processing.admin.core.detail = {};

endpoint.processing.admin.engine = {};

endpoint.processing.admin.engine.config = {};

endpoint.processing.admin.organization.detail = {};

endpoint.processing.admin.organization.tokens = {};

endpoint.processing.admin.users.permission.detail = {};

endpoint.processing.admin.users.role.detail = {};

endpoint.processing.admin.service.config = {};

endpoint.processing.admin.service.instances = {};

endpoint.processing.admin.status = {};

endpoint.processing.admin.token.detail = {};

endpoint.processing.admin.token.permissions = {};

endpoint.processing.admin.user.detail = {};

endpoint.processing.admin.user.permissions = {};

endpoint.processing.admin.user.tokens = {};

endpoint.processing.admin.users.permissions = {};

endpoint.processing.admin.users.roles = {};

endpoint.processing.admin.backlog.summary = {};

endpoint.processing.admin.config = {};

endpoint.processing.admin.resource.engines = {};

endpoint.processing.admin.licenses.detail = {};

endpoint.processing.admin.resource.mappings = {};

endpoint.processing.admin.resource.mapping = {};

endpoint.processing.admin.resource.mapping.detail = {};

endpoint.processing.admin.resources = {};

endpoint.processing.admin.engine.resources = {};

endpoint.processing.admin.host = {};

endpoint.processing.admin.host.resources = {};

endpoint.processing.admin.service.resources = {};

endpoint.processing.admin.serverType.detail = {};

endpoint.processing.admin.serverTypes.runningEngines = {};

endpoint.processing.admin.serverType.engine.detail = {};

endpoint.processing.admin.service.detail = {};

endpoint.processing.admin.users.login = {};

endpoint.processing.admin.users.oauth2Login = {};

endpoint.processing.admin.users.register = {};

endpoint.processing.admin.resource.engine.delete = {};

endpoint.processing.admin.serverType.remove = {};

endpoint.processing.admin.backlog.resetAlloc = {};

endpoint.processing.admin.users.signup = {};

endpoint.processing.admin.users.signup.initial = {};

endpoint.processing.admin.core.update = {};

endpoint.processing.admin.applications.update = {};

endpoint.processing.admin.resource.engine.update = {};

endpoint.processing.admin.organization.update = {};

endpoint.processing.admin.users.permission.update = {};

endpoint.processing.admin.resource.update = {};

endpoint.processing.admin.resource.mapping.update = {};

endpoint.processing.admin.resource.mapping.updatestate = {};

endpoint.processing.admin.resource.updatestate = {};

endpoint.processing.admin.users.role.update = {};

endpoint.processing.admin.serverType.update = {};

endpoint.processing.admin.serverType.engine.update = {};

endpoint.processing.admin.service.update = {};

endpoint.processing.admin.token.update = {};

endpoint.processing.admin.user.update = {};

endpoint.processing.admin.users.password = {};

endpoint.processing.admin.users.password.forgot = {};

endpoint.processing.admin.users.password.reset = {};

endpoint.processing.admin.users.password.update = {};

endpoint.processing.admin.build = {};

endpoint.processing.admin.build.create = {};

endpoint.processing.admin.engine.create = {};

endpoint.processing.admin.engine.category = {};

endpoint.processing.admin.engine.category.create = {};

endpoint.processing.engine = {};

endpoint.processing.engine.createjob = {};

endpoint.processing.admin.build.delete = {};

endpoint.processing.admin.engine.delete = {};

endpoint.processing.engine.template = {};

endpoint.processing.admin.build.detail = {};

endpoint.processing.admin.engine.builds = {};

endpoint.processing.engine.builds = {};

endpoint.processing.engine.builds.downloads = {};

endpoint.processing.admin.engine.categories = {};

endpoint.processing.admin.engine.category.detail = {};

endpoint.processing.engine.containerCount = {};

endpoint.processing.admin.engine.detail = {};

endpoint.processing.engine.instance = {};

endpoint.processing.engine.instance.detail = {};

endpoint.processing.engine.instances = {};

endpoint.processing.engine.instance.logs = {};

endpoint.processing.engine.instance.status = {};

endpoint.processing.engine.instance.getwork = {};

endpoint.processing.engine.instance.workdetail = {};

endpoint.processing.engine.launch = {};

endpoint.processing.engine.launch.detail = {};

endpoint.processing.engine.launches = {};

endpoint.processing.admin.engine.stats = {};

endpoint.processing.admin.engines = {};

endpoint.processing.proc = {};

endpoint.processing.proc.jobs = {};

endpoint.processing.proc.jobs.backlogCountByEngine = {};

endpoint.processing.engine.instance.resources = {};

endpoint.processing.admin.build.pause = {};

endpoint.processing.admin.engine.pause = {};

endpoint.processing.engine.instance.register = {};

endpoint.processing.admin.engine.replace = {};

endpoint.processing.admin.build.resume = {};

endpoint.processing.admin.engine.resume = {};

endpoint.processing.engine.instance.terminate = {};

endpoint.processing.admin.engine.update = {};

endpoint.processing.admin.build.update = {};

endpoint.processing.engine.build = {};

endpoint.processing.engine.build.state = {};

endpoint.processing.admin.engine.category.update = {};

endpoint.processing.engine.instance.updatestatus = {};

endpoint.processing.flowaction = {};

endpoint.processing.flowaction.requests = {};

endpoint.processing.flowaction.requests.clean = {};

endpoint.processing.flow = {};

endpoint.processing.flow.nrcontainer = {};

endpoint.processing.flow.nrcontainer.create = {};

endpoint.processing.flow.delete = {};

endpoint.processing.automate = {};

endpoint.processing.automate.containers = {};

endpoint.processing.flow.request = {};

endpoint.processing.flow.request.update = {};

endpoint.processing.flow.updatestatus = {};

endpoint.processing.host = {};

endpoint.processing.host.drain = {};

endpoint.processing.hosts = {};

endpoint.processing.hosts.cpuUsage = {};

endpoint.processing.host.detail = {};

endpoint.processing.engine.memoryUsage = {};

endpoint.processing.host.status = {};

endpoint.processing.hosts.counts = {};

endpoint.processing.hosts.countsByType = {};

endpoint.processing.host.launch = {};

endpoint.processing.host.alert = {};

endpoint.processing.host.pause = {};

endpoint.processing.host.engine = {};

endpoint.processing.host.engine.register = {};

endpoint.processing.host.register = {};

endpoint.processing.host.resume = {};

endpoint.processing.host.terminate = {};

endpoint.processing.host.updatestatus = {};

endpoint.processing.proc.job = {};

endpoint.processing.proc.job.addtask = {};

endpoint.processing.proc.job.addtaskroute = {};

endpoint.processing.proc.jobs.cancel = {};

endpoint.processing.proc.job.create = {};

endpoint.processing.proc.job.delete = {};

endpoint.processing.proc.scheduledJob = {};

endpoint.processing.proc.scheduledJob.delete = {};

endpoint.processing.proc.task = {};

endpoint.processing.proc.task.delete = {};

endpoint.processing.proc.tasks = {};

endpoint.processing.proc.tasks.error = {};

endpoint.processing.proc.tasks.error.engineErrorTimeRanges = {};

endpoint.processing.proc.job.detail = {};

endpoint.processing.proc.job.download = {};

endpoint.processing.proc.job.output = {};

endpoint.processing.proc.job.outputs = {};

endpoint.processing.proc.job.performance = {};

endpoint.processing.proc.job.status = {};

endpoint.processing.proc.job.workrequests = {};

endpoint.processing.proc.jobs.stats = {};

endpoint.processing.proc.jobs.stats.organization = {};

endpoint.processing.proc.jobs.performance = {};

endpoint.processing.proc.jobs.stats.timeRanges = {};

endpoint.processing.proc.scheduledJobs = {};

endpoint.processing.proc.scheduledJobs.ratePendingTimeRanges = {};

endpoint.processing.proc.scheduledJob.detail = {};

endpoint.processing.proc.tasks.error.serverErrorCounts = {};

endpoint.processing.proc.task.detail = {};

endpoint.processing.proc.task.download = {};

endpoint.processing.proc.task.logs = {};

endpoint.processing.proc.task.output = {};

endpoint.processing.proc.task.outputs = {};

endpoint.processing.proc.task.reports = {};

endpoint.processing.proc.tasks.syncBacklog = {};

endpoint.processing.proc.task.timings = {};

endpoint.processing.proc.tasks.error.countTypeByEngine = {};

endpoint.processing.proc.tasks.errors = {};

endpoint.processing.proc.tasks.instances = {};

endpoint.processing.proc.tasks.instances.byCategoryHost = {};

endpoint.processing.proc.tasks.stats = {};

endpoint.processing.proc.tasks.stats.categories = {};

endpoint.processing.proc.tasks.stats.engines = {};

endpoint.processing.proc.tasks.stats.organizations = {};

endpoint.processing.proc.tasks.stats.timeRanges = {};

endpoint.processing.proc.scheduledJobs.totalPendingTimeRanges = {};

endpoint.processing.proc.workrequest = {};

endpoint.processing.proc.workrequest.detail = {};

endpoint.processing.proc.template = {};

endpoint.processing.proc.template.launch = {};

endpoint.processing.proc.task.listreports = {};

endpoint.processing.proc.endpoint = {};

endpoint.processing.proc.job.recheckStatus = {};

endpoint.processing.proc.tasks.stats.export = {};

endpoint.processing.proc.tasks.updateStatus = {};

endpoint.processing.proxy = {};

endpoint.processing.proxy.getwork = {};

endpoint.processing.proxy.register = {};

endpoint.processing.proxy.terminate = {};

endpoint.processing.proxy.update = {};

endpoint.processing.security = {};

endpoint.processing.security.getstorageserverpublickeyset = {};

endpoint.processing.service = {};

endpoint.processing.service.instance = {};

endpoint.processing.service.instance.create = {};

endpoint.processing.service.instance.delete = {};

endpoint.processing.service.instance.resources = {};

endpoint.aiData.graphql.post = function(query, variables, httpOptions) {
    if (!query || !variables) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [query,variables].');
        return;
    }
    var url = parse('/v3/graphql');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    httpOptions = httpOptions || {};
    httpOptions.query = query;
    httpOptions.variables = variables;
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.eventEndpoint.put = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/event-endpoint', [applicationId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.applications.eventEndpoint.delete = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/event-endpoint', [applicationId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.applications.get = function(applicationId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/api/admin/applications');
			break;
		case 1:
			url = parse('/api/admin/applications/:applicationId', [applicationId]);
			break;
		case 2:
            url = parse('/api/admin/applications/:applicationId', [applicationId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.applications.post = function(httpOptions) {
    var url = parse('/api/admin/applications');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.put = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId', [applicationId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.applications.delete = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId', [applicationId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.applications.submit.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/submit', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.approve.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/approve', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.reject.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/reject', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.deploy.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/deploy', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.disable.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/disable', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.applications.enable.post = function(applicationId, httpOptions) {
    if (!applicationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [applicationId].');
        return;
    }
    var url = parse('/api/admin/applications/:applicationId/enable', [applicationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.organizations.applications.patch = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/applications', [id]);
    sys.logs.debug('[veritoneuser] PATCH from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._patch(options);
};

endpoint.aiDataAdmin.organizations.applications.delete = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/applications', [id]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.externalCredentials.get = function(externalCredentialId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/api/admin/external-credentials');
			break;
		case 1:
			url = parse('/api/admin/external-credentials/:externalCredentialId', [externalCredentialId]);
			break;
		case 2:
            url = parse('/api/admin/external-credentials/:externalCredentialId', [externalCredentialId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.externalCredentials.post = function(httpOptions) {
    var url = parse('/api/admin/external-credentials');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.externalCredentials.put = function(externalCredentialId, httpOptions) {
    if (!externalCredentialId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [externalCredentialId].');
        return;
    }
    var url = parse('/api/admin/external-credentials/:externalCredentialId', [externalCredentialId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.externalCredentials.delete = function(externalCredentialId, httpOptions) {
    if (!externalCredentialId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [externalCredentialId].');
        return;
    }
    var url = parse('/api/admin/external-credentials/:externalCredentialId', [externalCredentialId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.integration.org.put = function(integrationId, orgId, httpOptions) {
    if (!integrationId || !orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [integrationId,orgId].');
        return;
    }
    var url = parse('/api/integration/:integrationId/org/:orgId', [integrationId, orgId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.currentUser.get = function(httpOptions) {
    var url = parse('/api/admin/current-user');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.me.applications.get = function(httpOptions) {
    var url = parse('/api/admin/users/me/applications');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.me.refreshUserCache.get = function(httpOptions) {
    var url = parse('/api/admin/users/me/refresh-user-cache');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.signup.assignVertical.post = function(httpOptions) {
    var url = parse('/api/admin/signup/assign-vertical');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.signup.assignVertical.cloneProgress.get = function(cloneId, httpOptions) {
    if (!cloneId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [cloneId].');
        return;
    }
    var url = parse('/api/admin/signup/assign-vertical/:cloneId/clone-progress', [cloneId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.login.post = function(httpOptions) {
    var url = parse('/api/admin/login');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.signup.initial.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/signup/initial/:token', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.token.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/token/:token', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.token.logout.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/token/:token/logout', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.token.refresh.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/token/:token/refresh', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.token.extend.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/token/:token/extend', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.password.requestReset.post = function(httpOptions) {
    var url = parse('/api/admin/password/request-reset');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.password.reset.post = function(httpOptions) {
    var url = parse('/api/admin/password/reset');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.passwordToken.post = function(httpOptions) {
    var url = parse('/api/admin/password-token');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.mfa.token.get = function(type, httpOptions) {
    if (!type) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [type].');
        return;
    }
    var url = parse('/api/admin/mfa/:type/token', [type]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.mfa.verify.post = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/mfa/verify/:token', [token]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.bearer.get = function(token, httpOptions) {
    if (!token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [token].');
        return;
    }
    var url = parse('/api/admin/bearer/:token', [token]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.orgInvite.password.reset.post = function(httpOptions) {
    var url = parse('/api/admin/org-invite/password/reset');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.reload.get = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id/reload', [id]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.openid.linkUserToOid.post = function(httpOptions) {
    var url = parse('/api/admin/openid/linkUserToOid');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.organization.integration.get = function(orgId, integrationId, httpOptions) {
    if (!orgId || !integrationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId,integrationId].');
        return;
    }
    var url = parse('/api/admin/organization/:orgId/integration/:integrationId', [orgId, integrationId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.groups.application.organization.get = function(httpOptions) {
    var url = parse('/api/admin/groups/application/organization');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.openidRoles.delete = function(id, openidRoleKey, httpOptions) {
    if (!id || !openidRoleKey) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id,openidRoleKey].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/openid-roles/:openidRoleKey', [id, openidRoleKey]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.organizations.openidRoles.put = function(id, openidRoleKey, httpOptions) {
    if (!id || !openidRoleKey) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id,openidRoleKey].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/openid-roles/:openidRoleKey', [id, openidRoleKey]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.organizations.openidRoles.get = function(id, openidRoleKey, httpOptions) {
    if (arguments.length === 0 || !id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }

    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length-1){
        case 0:
            url = parse('/api/admin/organizations/:id/openid-roles', [id]);
            break;
        case 1:
            url = parse('/api/admin/organizations/:id/openid-roles/:openidRoleKey', [id, openidRoleKey]);
            break;
        case 2:
            url = parse('/api/admin/organizations/:id/openid-roles/:openidRoleKey', [id,openidRoleKey]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.openidRoles.post = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/openid-roles', [id]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.organizations.integration.delete = function(id, integrationId, httpOptions) {
    if (!id || !integrationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id,integrationId].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/integration/:integrationId', [id, integrationId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.organizations.integration.put = function(id, integrationId, httpOptions) {
    if (!id || !integrationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id,integrationId].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/integration/:integrationId', [id, integrationId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.organizations.get = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId', [orgId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.put = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId', [orgId]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.organizations.roles.get = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId/roles', [orgId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.integrations.get = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/organizations/:id/integrations', [id]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.paymentToken.post = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId/paymentToken', [orgId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.organizations.paymentToken.get = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId/paymentToken', [orgId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.organizations.invoices.get = function(orgId, httpOptions) {
    if (!orgId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [orgId].');
        return;
    }
    var url = parse('/api/admin/organizations/:orgId/invoices', [orgId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.integration.get = function(integrationId, httpOptions) {
    if (!integrationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [integrationId].');
        return;
    }
    var url = parse('/api/admin/integration/:integrationId', [integrationId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.openid.user.connection.delete = function(userId, connectId, httpOptions) {
    if (!userId || !connectId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,connectId].');
        return;
    }
    var url = parse('/api/admin/openid/user/:userId/connection/:connectId', [userId, connectId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.permissions.get = function(permId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/api/admin/permissions');
			break;
		case 1:
			url = parse('/api/admin/permissions/:permId', [permId]);
			break;
		case 2:
            url = parse('/api/admin/permissions/:permId', [permId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.analytics.qlik.app.get = function(appId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/analytics/qlik/app');
			break;
		case 1:
			url = parse('/analytics/qlik/app/:appId', [appId]);
			break;
		case 2:
            url = parse('/analytics/qlik/app/:appId', [appId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.analytics.qlik.app.sheet.get = function(appId, httpOptions) {
    if (!appId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [appId].');
        return;
    }
    var url = parse('/analytics/qlik/app/:appId/sheet', [appId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.analytics.qlik.organization.init.post = function(organizationId, httpOptions) {
    if (!organizationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [organizationId].');
        return;
    }
    var url = parse('/analytics/qlik/organization/:organizationId/init', [organizationId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.analytics.qlik.stream.put = function(streamName, httpOptions) {
    if (!streamName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [streamName].');
        return;
    }
    var url = parse('/analytics/qlik/stream/:streamName', [streamName]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.tokens.organizations.tokens.all.get = function(organizationId, httpOptions) {
    if (!organizationId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [organizationId].');
        return;
    }
    var url = parse('/api/admin/tokens/organizations/:organizationId/tokens/all', [organizationId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.mfa.get = function(userId, httpOptions) {
    if (!userId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId].');
        return;
    }
    var url = parse('/api/admin/users/:userId/mfa', [userId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.mfa.register.post = function(userId, httpOptions) {
    if (!userId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId].');
        return;
    }
    var url = parse('/api/admin/users/:userId/mfa/register', [userId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.mfa.register.verify.post = function(userId, type, token, httpOptions) {
    if (!userId || !type || !token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,type,token].');
        return;
    }
    var url = parse('/api/admin/users/:userId/mfa/register/verify/:type/:token', [userId, type, token]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.mfa.default.put = function(userId, type, httpOptions) {
    if (!userId || !type) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,type].');
        return;
    }
    var url = parse('/api/admin/users/:userId/mfa/default/:type', [userId, type]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.users.mfa.unregister.post = function(userId, type, httpOptions) {
    if (!userId || !type) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,type].');
        return;
    }
    var url = parse('/api/admin/users/:userId/mfa/unregister/:type', [userId, type]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.roles.get = function(roleId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/api/admin/roles');
			break;
		case 1:
			url = parse('/api/admin/roles/:roleId', [roleId]);
			break;
		case 2:
            url = parse('/api/admin/roles/:roleId', [roleId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.roles.permissions.get = function(roleId, httpOptions) {
    if (!roleId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [roleId].');
        return;
    }
    var url = parse('/api/admin/roles/:roleId/permissions', [roleId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.signup.post = function(httpOptions) {
    var url = parse('/api/admin/signup');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.signup.resend.post = function(httpOptions) {
    var url = parse('/api/admin/signup/resend');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.signup.verify.post = function(httpOptions) {
    var url = parse('/api/admin/signup/verify');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.signup.verify.get = function(userName, httpOptions) {
    if (!userName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userName].');
        return;
    }
    var url = parse('/api/admin/signup/verify/:userName', [userName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.get = function(id, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/api/admin/users');
			break;
		case 1:
			url = parse('/api/admin/users/:id', [id]);
			break;
		case 2:
            url = parse('/api/admin/users/:id', [id]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.aiDataAdmin.users.delete = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id', [id]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.users.put = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id', [id]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.users.post = function(httpOptions) {
    var url = parse('/api/admin/users');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.permissions.get = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id/permissions', [id]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.roles.post = function(userId, roleId, httpOptions) {
    if (!userId || !roleId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,roleId].');
        return;
    }
    var url = parse('/api/admin/users/:userId/roles/:roleId', [userId, roleId]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.roles.delete = function(userId, roleId, httpOptions) {
    if (!userId || !roleId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [userId,roleId].');
        return;
    }
    var url = parse('/api/admin/users/:userId/roles/:roleId', [userId, roleId]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.users.forcePasswordReset.post = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id/force-password-reset', [id]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.scimConnectIds.get = function(id, httpOptions) {
    if (!id) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [id].');
        return;
    }
    var url = parse('/api/admin/users/:id/scim_connect_ids', [id]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.me.userSettings.get = function(httpOptions) {
    var url = parse('/api/admin/users/me/user-settings');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.me.userSettings.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/user-settings');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.userSettings.put = function(httpOptions) {
    var url = parse('/api/admin/users/me/user-settings');
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.aiDataAdmin.users.me.acceptTerms.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/accept-terms');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.acceptDevTerms.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/accept-dev-terms');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.userSettings.delete = function(key, httpOptions) {
    if (!key) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [key].');
        return;
    }
    var url = parse('/api/admin/users/me/user-settings/:key', [key]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.aiDataAdmin.users.me.changePassword.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/change-password');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.setDeveloperOrganizationFields.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/set-developer-organization-fields');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.mfa.get = function(httpOptions) {
    var url = parse('/api/admin/users/me/mfa');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.aiDataAdmin.users.me.mfa.register.post = function(httpOptions) {
    var url = parse('/api/admin/users/me/mfa/register');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.mfa.register.verify.post = function(type, token, httpOptions) {
    if (!type || !token) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [type,token].');
        return;
    }
    var url = parse('/api/admin/users/me/mfa/register/verify/:type/:token', [type, token]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.users.me.mfa.unregister.post = function(type, httpOptions) {
    if (!type) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [type].');
        return;
    }
    var url = parse('/api/admin/users/me/mfa/unregister/:type', [type]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.aiDataAdmin.username.available.get = function(email, httpOptions) {
    if (!email) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [email].');
        return;
    }
    var url = parse('/api/admin/username/:email/available', [email]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.project.post = function(httpOptions) {
    var url = parse('/v2/project');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.project.get = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid', [projectid]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.projects.get = function(httpOptions) {
    var url = parse('/v2/projects');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.project.put = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid', [projectid]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.voice.project.delete = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid', [projectid]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.voice.project.clips.get = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid/clips', [projectid]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.project.tts.clip.post = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip', [projectid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.project.tts.clip.get = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip/:clipid', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.project.tts.clip.put = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip/:clipid', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.voice.project.tts.clip.upload.put = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip/:clipid/upload', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.voice.project.tts.clip.delete = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip/:clipid', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.voice.project.sts.clip.post = function(projectid, httpOptions) {
    if (!projectid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid].');
        return;
    }
    var url = parse('/v2/project/:projectid/sts/clip', [projectid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.project.sts.clip.get = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/sts/clip/:clipid', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.project.sts.clip.delete = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/sts/clip/:clipid', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.voice.sts.voices.get = function(httpOptions) {
    var url = parse('/v2/sts/voices?lang');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.tts.preview.post = function(httpOptions) {
    var url = parse('/v2/tts/preview');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.project.tts.clip.convert.post = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/tts/clip/:clipid/convert', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.project.sts.clip.convert.post = function(projectid, clipid, httpOptions) {
    if (!projectid || !clipid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [projectid,clipid].');
        return;
    }
    var url = parse('/v2/project/:projectid/sts/clip/:clipid/convert', [projectid, clipid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.lexicon.post = function(httpOptions) {
    var url = parse('/v2/lexicon');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.lexicon.get = function(lexiconId, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/v2/lexicon');
			break;
		case 1:
			url = parse('/v2/lexicon/:lexicon_id', [lexiconId]);
			break;
		case 2:
            url = parse('/v2/lexicon/:lexicon_id', [lexiconId]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.voice.lexicon.put = function(lexiconid, httpOptions) {
    if (!lexiconid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid].');
        return;
    }
    var url = parse('/v2/lexicon/:lexiconid', [lexiconid]);
    sys.logs.debug('[veritoneuser] PUT from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options);
};

endpoint.voice.lexicon.delete = function(lexiconid, httpOptions) {
    if (!lexiconid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid].');
        return;
    }
    var url = parse('/v2/lexicon/:lexiconid', [lexiconid]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.voice.lexicon.lexeme.post = function(lexiconid, httpOptions) {
    if (!lexiconid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid].');
        return;
    }
    var url = parse('/v2/lexicon/:lexiconid/lexeme', [lexiconid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.lexicon.lexeme.get = function(lexiconid, lexemeid, httpOptions) {
    if (!lexiconid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid].');
        return;
    }
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length-1){
        case 0:
            url = parse('/v2/lexicon/:lexiconid/lexeme', [lexiconid]);
            break;
        case 1:
            url = parse('/v2/lexicon/:lexiconid/lexeme/:lexemeid', [lexiconid, lexemeid]);
            break;
        case 2:
            url = parse('/v2/lexicon/:lexiconid/lexeme/:lexemeid', [lexiconid,lexemeid]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.voice.lexicon.lexeme.delete = function(lexiconid, lexemeid, httpOptions) {
    if (!lexiconid || !lexemeid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid,lexemeid].');
        return;
    }
    var url = parse('/v2/lexicon/:lexiconid/lexeme/:lexemeid', [lexiconid, lexemeid]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.voice.lexicon.lexeme.preview.post = function(lexiconid, httpOptions) {
    if (!lexiconid) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [lexiconid].');
        return;
    }
    var url = parse('/v2/lexicon/:lexiconid/lexeme/preview', [lexiconid]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.voice.tts.voice.get = function(voiceId, httpOptions) {
    if (arguments.length === 0 || !voiceId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [httpOptions or voiceId].');
        return;
    }
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    var options;
    switch(arguments.length-1){
        case 0:
            url = parse('/v2/tts/voice?lang');
            options = checkHttpOptions(url, voiceId);
            break;
        case 1:
            url = parse('/v2/tts/voice/:voice_id', [voiceId]);
            options = checkHttpOptions(url, httpOptions);
            break;
        case 2:
            url = parse('/v2/tts/voice/:voice_id', [voiceId]);
            options = checkHttpOptions(url, httpOptions);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    return endpoint._get(options);
};

endpoint.voice.tts.voice.versions.get = function(voiceId, httpOptions) {
    if (!voiceId) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [voiceId].');
        return;
    }
    var url = parse('/v2/tts/voice/:voice_id/versions', [voiceId]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.resource.engine.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/engine', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.service.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/service', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.add.post = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/add', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.terminate.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/terminate');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.licenses.apply.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/licenses/apply');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.auth.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/auth');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.backlog.clear.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/backlog/clear');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.applications.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/applications/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.cores.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/cores/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.organizations.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/organizations/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.permission.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/permission/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/resource/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.role.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/role/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverTypes.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/server_types/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverTypes.runningEngine.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/server_types/running_engine/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.service.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/service/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.tokens.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/tokens/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.applications.delete.post = function(ApplicationID, httpOptions) {
    if (!ApplicationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ApplicationID].');
        return;
    }
    var url = parse('/edge/v1/admin/applications/:ApplicationID/delete', [ApplicationID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.licenses.delete.delete = function(LicenseID, httpOptions) {
    if (!LicenseID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [LicenseID].');
        return;
    }
    var url = parse('/edge/v1/admin/licenses/:LicenseID/delete', [LicenseID]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.processing.admin.organization.delete.post = function(OrganizationID, httpOptions) {
    if (!OrganizationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [OrganizationID].');
        return;
    }
    var url = parse('/edge/v1/admin/organization/:OrganizationID/delete', [OrganizationID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.delete.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/delete', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.role.delete.post = function(RoleID, httpOptions) {
    if (!RoleID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [RoleID].');
        return;
    }
    var url = parse('/edge/v1/admin/users/role/:RoleID/delete', [RoleID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.delete.delete = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/delete', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.processing.admin.serverType.delete.post = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/delete', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.engine.delete.delete = function(ServerTypeID, EngineID, httpOptions) {
    if (!ServerTypeID || !EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID,EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/engine/:EngineID/delete', [ServerTypeID, EngineID]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.processing.admin.services.delete.post = function(ServiceID, httpOptions) {
    if (!ServiceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceID].');
        return;
    }
    var url = parse('/edge/v1/admin/services/:ServiceID/delete', [ServiceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.token.delete.post = function(TokenID, httpOptions) {
    if (!TokenID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TokenID].');
        return;
    }
    var url = parse('/edge/v1/admin/token/:TokenID/delete', [TokenID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.user.delete.post = function(UserID, httpOptions) {
    if (!UserID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [UserID].');
        return;
    }
    var url = parse('/edge/v1/admin/user/:UserID/delete', [UserID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.desired.post = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/desired', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.application.detail.get = function(ApplicationID, httpOptions) {
    if (!ApplicationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ApplicationID].');
        return;
    }
    var url = parse('/edge/v1/admin/application/:ApplicationID/detail', [ApplicationID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.core.detail.get = function(CoreID, httpOptions) {
    if (!CoreID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [CoreID].');
        return;
    }
    var url = parse('/edge/v1/admin/core/:CoreID/detail', [CoreID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.cores.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/cores');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.config.get = function(EngineID, ConfigSection, ConfigKey, httpOptions) {
    if (!EngineID || !ConfigSection || !ConfigKey) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID,ConfigSection,ConfigKey].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/config/:ConfigSection/:ConfigKey', [EngineID, ConfigSection, ConfigKey]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.organization.detail.get = function(OrganizationID, httpOptions) {
    if (!OrganizationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [OrganizationID].');
        return;
    }
    var url = parse('/edge/v1/admin/organization/:OrganizationID/detail', [OrganizationID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.organization.tokens.get = function(OrganizationID, httpOptions) {
    if (!OrganizationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [OrganizationID].');
        return;
    }
    var url = parse('/edge/v1/admin/organization/:OrganizationID/tokens', [OrganizationID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.organizations.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/organizations');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.permission.detail.get = function(PermissionID, httpOptions) {
    if (!PermissionID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [PermissionID].');
        return;
    }
    var url = parse('/edge/v1/admin/users/permission/:PermissionID/detail', [PermissionID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.role.detail.get = function(RoleID, httpOptions) {
    if (!RoleID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [RoleID].');
        return;
    }
    var url = parse('/edge/v1/admin/users/role/:RoleID/detail', [RoleID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.service.config.get = function(ServiceID, ConfigSection, ConfigKey, httpOptions) {
    if (arguments.length === 0){
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceID].');
        return;
    }

    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length-1){
        case 0:
            url = parse('/edge/v1/admin/service/:ServiceID/config', [ServiceID]);
            break;
        case 1:
            url = parse('/edge/v1/admin/service/:ServiceID/config/:ConfigSection', [ServiceID, ConfigSection]);
            break;
        case 2:
            url = parse('/edge/v1/admin/service/:ServiceID/config/:ConfigSection/:ConfigKey', [ServiceID, ConfigSection, ConfigKey]);
            break;
        case 3:
            url = parse('/edge/v1/admin/service/:ServiceID/config/:ConfigSection/:ConfigKey', [ServiceID,ConfigSection,ConfigKey]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.service.instances.get = function(ServiceID, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/edge/v1/admin/service/instances');
			break;
		case 1:
			url = parse('/edge/v1/admin/service/:ServiceID/instances', [ServiceID]);
			break;
		case 2:
            url = parse('/edge/v1/admin/service/:ServiceID/instances', [ServiceID]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.processing.admin.status.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/status');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.token.detail.get = function(TokenID, httpOptions) {
    if (!TokenID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TokenID].');
        return;
    }
    var url = parse('/edge/v1/admin/token/:TokenID/detail', [TokenID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.token.permissions.get = function(TokenID, httpOptions) {
    if (!TokenID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TokenID].');
        return;
    }
    var url = parse('/edge/v1/admin/token/:TokenID/permissions', [TokenID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.tokens.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/tokens');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.user.detail.get = function(UserID, httpOptions) {
    if (!UserID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [UserID].');
        return;
    }
    var url = parse('/edge/v1/admin/user/:UserID/detail', [UserID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.user.permissions.get = function(UserID, httpOptions) {
    if (!UserID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [UserID].');
        return;
    }
    var url = parse('/edge/v1/admin/user/:UserID/permissions', [UserID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.user.tokens.get = function(UserID, httpOptions) {
    if (!UserID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [UserID].');
        return;
    }
    var url = parse('/edge/v1/admin/user/:UserID/tokens', [UserID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/users');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.permissions.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/permissions');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.roles.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/roles');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.applications.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/applications');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.backlog.summary.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/backlog/summary');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.config.get = function(ConfigSection, ConfigKey, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/edge/v1/admin/config');
			break;
		case 1:
			url = parse('/edge/v1/admin/config/:ConfigSection', [ConfigSection]);
			break;
		case 2:
			url = parse('/edge/v1/admin/config/:ConfigSection/:ConfigKey', [ConfigSection, ConfigKey]);
			break;
		case 3:
            url = parse('/edge/v1/admin/config/:ConfigSection/:ConfigKey', [ConfigSection,ConfigKey]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.processing.admin.resource.engines.get = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/engines', [ResourceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.licenses.detail.get = function(LicenseID, httpOptions) {
    if (!LicenseID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [LicenseID].');
        return;
    }
    var url = parse('/edge/v1/admin/licenses/:LicenseID/detail', [LicenseID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.licenses.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/licenses');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.resource.mappings.get = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/mappings', [ResourceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.resource.get = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID', [ResourceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.resource.mapping.detail.get = function(ResourceID, ResourceMappingID, httpOptions) {
    if (!ResourceID || !ResourceMappingID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID,ResourceMappingID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/mapping/:ResourceMappingID/detail', [ResourceID, ResourceMappingID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.resources.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/resources');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.resources.get = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/resources', [EngineID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.host.resources.get = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/admin/host/:HostID/resources', [HostID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.service.resources.get = function(ServiceID, httpOptions) {
    if (!ServiceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceID].');
        return;
    }
    var url = parse('/edge/v1/admin/service/:ServiceID/resources', [ServiceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.serverType.detail.get = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/detail', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.serverTypes.runningEngines.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/server_types/running_engines');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.serverType.engine.detail.get = function(ServerTypeID, EngineID, httpOptions) {
    if (!ServerTypeID || !EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID,EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/engine/:EngineID/detail', [ServerTypeID, EngineID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.serverTypes.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/server_types');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.service.detail.get = function(ServiceID, httpOptions) {
    if (!ServiceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceID].');
        return;
    }
    var url = parse('/edge/v1/admin/service/:ServiceID/detail', [ServiceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.services.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/services');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.users.login.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/login');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.oauth2Login.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/oauth2_login');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.register.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/register');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.engine.delete.post = function(ResourceID, EngineID, httpOptions) {
    if (!ResourceID || !EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID,EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/engine/:EngineID/delete', [ResourceID, EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.remove.post = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/remove', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.backlog.resetAlloc.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/backlog/reset-alloc');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.signup.initial.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/signup/initial');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.core.update.post = function(CoreID, httpOptions) {
    if (!CoreID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [CoreID].');
        return;
    }
    var url = parse('/edge/v1/admin/core/:CoreID/update', [CoreID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.applications.update.post = function(ApplicationID, httpOptions) {
    if (!ApplicationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ApplicationID].');
        return;
    }
    var url = parse('/edge/v1/admin/applications/:ApplicationID/update', [ApplicationID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.config.post = function(ConfigSection, ConfigKey, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 1:
			url = parse('/edge/v1/admin/config');
			break;
		case 2:
			url = parse('/edge/v1/admin/config/:ConfigSection', [ConfigSection]);
			break;
		case 3:
			url = parse('/edge/v1/admin/config/:ConfigSection/:ConfigKey', [ConfigSection, ConfigKey]);
			break;
		case 4:
            url = parse('/edge/v1/admin/config/:ConfigSection/:ConfigKey', [ConfigSection,ConfigKey,body]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] POST from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._post(options);
};

endpoint.processing.admin.resource.engine.update.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/engine/update', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.organization.update.post = function(OrganizationID, httpOptions) {
    if (!OrganizationID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [OrganizationID].');
        return;
    }
    var url = parse('/edge/v1/admin/organization/:OrganizationID/update', [OrganizationID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.permission.update.post = function(PermissionID, httpOptions) {
    if (!PermissionID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [PermissionID].');
        return;
    }
    var url = parse('/edge/v1/admin/users/permission/:PermissionID/update', [PermissionID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.update.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/update', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.mapping.update.post = function(ResourceID, ResourceMappingID, httpOptions) {
    if (!ResourceID || !ResourceMappingID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID,ResourceMappingID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/mapping/:ResourceMappingID/update', [ResourceID, ResourceMappingID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.mapping.updatestate.post = function(ResourceID, ResourceMappingID, httpOptions) {
    if (!ResourceID || !ResourceMappingID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID,ResourceMappingID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/mapping/:ResourceMappingID/updatestate', [ResourceID, ResourceMappingID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.resource.updatestate.post = function(ResourceID, httpOptions) {
    if (!ResourceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ResourceID].');
        return;
    }
    var url = parse('/edge/v1/admin/resource/:ResourceID/updatestate', [ResourceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.role.update.post = function(RoleID, httpOptions) {
    if (!RoleID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [RoleID].');
        return;
    }
    var url = parse('/edge/v1/admin/users/role/:RoleID/update', [RoleID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.update.post = function(ServerTypeID, httpOptions) {
    if (!ServerTypeID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/update', [ServerTypeID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.serverType.engine.update.post = function(ServerTypeID, EngineID, httpOptions) {
    if (!ServerTypeID || !EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServerTypeID,EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/server_type/:ServerTypeID/engine/:EngineID/update', [ServerTypeID, EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.service.update.post = function(ServiceID, httpOptions) {
    if (!ServiceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceID].');
        return;
    }
    var url = parse('/edge/v1/admin/service/:ServiceID/update', [ServiceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.token.update.post = function(TokenID, httpOptions) {
    if (!TokenID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TokenID].');
        return;
    }
    var url = parse('/edge/v1/admin/token/:TokenID/update', [TokenID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.user.update.post = function(UserID, httpOptions) {
    if (!UserID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [UserID].');
        return;
    }
    var url = parse('/edge/v1/admin/user/:UserID/update', [UserID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.password.forgot.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/password/forgot');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.password.reset.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/password/reset');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.users.password.update.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/users/password/update');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.build.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/build/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/engine/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.category.create.post = function(httpOptions) {
    var url = parse('/edge/v1/admin/engine/category/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.createjob.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/createjob', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.build.delete.post = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/admin/build/:BuildID/delete', [BuildID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.delete.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/delete', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.template.delete = function(EngineID, EngineTemplateName, httpOptions) {
    if (!EngineID || !EngineTemplateName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID,EngineTemplateName].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/template/:EngineTemplateName', [EngineID, EngineTemplateName]);
    sys.logs.debug('[veritoneuser] DELETE from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options);
};

endpoint.processing.admin.build.detail.get = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/admin/build/:BuildID/detail', [BuildID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.builds.get = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/builds', [EngineID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.builds.get = function(httpOptions) {
    var url = parse('/edge/v1/engine/builds');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.builds.downloads.get = function(httpOptions) {
    var url = parse('/edge/v1/engine/builds/downloads');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.categories.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/engine/categories');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.category.detail.get = function(EngineCategoryID, httpOptions) {
    if (!EngineCategoryID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineCategoryID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/category/:EngineCategoryID/detail', [EngineCategoryID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.containerCount.get = function(httpOptions) {
    var url = parse('/edge/v1/engine/container_count');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.detail.get = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/detail', [EngineID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.instance.detail.get = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/detail', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.instances.get = function(EngineID, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/edge/v1/engine/instances');
			break;
		case 1:
			url = parse('/edge/v1/engine/:EngineID/instances', [EngineID]);
			break;
		case 2:
            url = parse('/edge/v1/engine/:EngineID/instances', [EngineID]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.processing.engine.instance.logs.get = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/logs', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.instance.status.get = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/status', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.instance.getwork.post = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/getwork', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.instance.workdetail.get = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/workdetail', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.launch.detail.get = function(EngineID, LaunchID, httpOptions) {
    if (!EngineID || !LaunchID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID,LaunchID].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/launch/:LaunchID/detail', [EngineID, LaunchID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.launches.get = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/launches', [EngineID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engine.stats.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/engine/stats');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.template.get = function(EngineID, EngineTemplateName, httpOptions) {
    if (!EngineID || !EngineTemplateName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID,EngineTemplateName].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/template/:EngineTemplateName', [EngineID, EngineTemplateName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.engines.get = function(httpOptions) {
    var url = parse('/edge/v1/admin/engines');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.backlogCountByEngine.post = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/backlog_count_by_engine');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.instance.resources.get = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/resources', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.admin.build.pause.post = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/admin/build/:BuildID/pause', [BuildID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.pause.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/pause', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.instance.register.post = function(httpOptions) {
    var url = parse('/edge/v1/engine/instance/register');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.replace.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/replace', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.build.resume.post = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/admin/build/:BuildID/resume', [BuildID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.resume.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/resume', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.instance.terminate.post = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/terminate', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.update.post = function(EngineID, httpOptions) {
    if (!EngineID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/:EngineID/update', [EngineID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.build.update.post = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/admin/build/:BuildID/update', [BuildID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.build.state.post = function(BuildID, httpOptions) {
    if (!BuildID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [BuildID].');
        return;
    }
    var url = parse('/edge/v1/engine/build/:BuildID/state', [BuildID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.admin.engine.category.update.post = function(EngineCategoryID, httpOptions) {
    if (!EngineCategoryID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineCategoryID].');
        return;
    }
    var url = parse('/edge/v1/admin/engine/category/:EngineCategoryID/update', [EngineCategoryID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.instance.updatestatus.post = function(EngineInstanceID, httpOptions) {
    if (!EngineInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineInstanceID].');
        return;
    }
    var url = parse('/edge/v1/engine/instance/:EngineInstanceID/updatestatus', [EngineInstanceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.engine.template.post = function(EngineID, EngineTemplateName, httpOptions) {
    if (!EngineID || !EngineTemplateName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [EngineID,EngineTemplateName].');
        return;
    }
    var url = parse('/edge/v1/engine/:EngineID/template/:EngineTemplateName', [EngineID, EngineTemplateName]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.flowaction.requests.clean.get = function(httpOptions) {
    var url = parse('/edge/v1/flowaction/requests/clean');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.flow.nrcontainer.create.post = function(httpOptions) {
    var url = parse('/edge/v1/flow/nrcontainer/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.flow.delete.post = function(ContainerID, httpOptions) {
    if (!ContainerID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ContainerID].');
        return;
    }
    var url = parse('/edge/v1/flow/delete/:ContainerID', [ContainerID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.automate.containers.get = function(httpOptions) {
    var url = parse('/edge/v1/automate/containers');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.flow.request.update.post = function(httpOptions) {
    var url = parse('/edge/v1/flow/request/update');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.flow.updatestatus.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/flow/:HostID/updatestatus', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.drain.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/drain', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.hosts.cpuUsage.get = function(httpOptions) {
    var url = parse('/edge/v1/hosts/cpu_usage');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.host.detail.get = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/detail', [HostID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.engine.memoryUsage.get = function(httpOptions) {
    var url = parse('/edge/v1/engine/memory_usage');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.host.status.get = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/status', [HostID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.hosts.get = function(httpOptions) {
    var url = parse('/edge/v1/hosts');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.hosts.counts.get = function(httpOptions) {
    var url = parse('/edge/v1/hosts/counts');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.hosts.countsByType.get = function(httpOptions) {
    var url = parse('/edge/v1/hosts/counts_by_type');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.host.launch.get = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/launch', [HostID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.host.alert.post = function(httpOptions) {
    var url = parse('/edge/v1/host/alert');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.launch.post = function(httpOptions) {
    var url = parse('/edge/v1/host/launch');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.pause.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/pause', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.engine.register.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/engine/register', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.register.post = function(httpOptions) {
    var url = parse('/edge/v1/host/register');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.resume.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/resume', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.terminate.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/terminate', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.host.updatestatus.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/host/:HostID/updatestatus', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.job.addtask.post = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/addtask', [JobID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.job.addtaskroute.post = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/addtaskroute', [JobID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.jobs.cancel.post = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/cancel');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.job.create.post = function(httpOptions) {
    var url = parse('/edge/v1/proc/job/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.job.delete.post = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/delete', [JobID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.scheduledJob.delete.post = function(ScheduledJobID, httpOptions) {
    if (!ScheduledJobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ScheduledJobID].');
        return;
    }
    var url = parse('/edge/v1/proc/scheduled_job/:ScheduledJobID/delete', [ScheduledJobID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.task.delete.post = function(TaskID, httpOptions) {
    if (!TaskID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/delete', [TaskID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.tasks.error.engineErrorTimeRanges.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/error/engine_error_time_ranges');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.detail.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/detail', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.download.get = function(JobID, JobOutputName, httpOptions) {
    if (!JobID || !JobOutputName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID,JobOutputName].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/download/:JobOutputName', [JobID, JobOutputName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.output.get = function(JobID, JobOutputName, httpOptions) {
    if (!JobID || !JobOutputName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID,JobOutputName].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/output/:JobOutputName', [JobID, JobOutputName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.outputs.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/outputs', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.performance.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/performance', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.status.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/status', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.job.workrequests.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/workrequests', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.stats.organization.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/stats/organization');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.performance.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/performance');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.stats.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/stats');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.jobs.stats.timeRanges.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/jobs/stats/time_ranges');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.scheduledJobs.ratePendingTimeRanges.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/scheduled_jobs/rate_pending_time_ranges');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.scheduledJob.detail.get = function(ScheduledJobID, httpOptions) {
    if (!ScheduledJobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ScheduledJobID].');
        return;
    }
    var url = parse('/edge/v1/proc/scheduled_job/:ScheduledJobID/detail', [ScheduledJobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.scheduledJobs.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/scheduled_jobs');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.error.serverErrorCounts.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/error/server_error_counts');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.detail.get = function(TaskID, httpOptions) {
    if (!TaskID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/detail', [TaskID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.download.get = function(TaskID, TaskOutputName, httpOptions) {
    if (!TaskID || !TaskOutputName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID,TaskOutputName].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/download/:TaskOutputName', [TaskID, TaskOutputName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.logs.get = function(TaskID, httpOptions) {
    if (!TaskID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/logs', [TaskID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.output.get = function(TaskID, TaskOutputName, httpOptions) {
    if (!TaskID || !TaskOutputName) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID,TaskOutputName].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/output/:TaskOutputName', [TaskID, TaskOutputName]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.outputs.get = function(TaskID, httpOptions) {
    if (!TaskID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/outputs', [TaskID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.reports.get = function(ReportMonth, httpOptions) {
    if(!httpOptions){
        for (var i = 0 ; i < arguments.length; i++){
            if (isObject(arguments[i])){
                httpOptions = arguments[i];
            }
        }
    }
    var url;
    switch(arguments.length){
        case 0:
			url = parse('/edge/v1/proc/task/reports');
			break;
		case 1:
			url = parse('/edge/v1/proc/task/reports/:ReportMonth', [ReportMonth]);
			break;
		case 2:
            url = parse('/edge/v1/proc/task/reports/:ReportMonth', [ReportMonth]);
            break;
        default:
            sys.logs.error('Invalid argument received.');
            return;
    }
    sys.logs.debug('[veritoneuser] GET from: ' + url);
	var options = checkHttpOptions(url, httpOptions);
	return endpoint._get(options);
};

endpoint.processing.proc.tasks.syncBacklog.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/sync_backlog');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.task.timings.get = function(TaskID, httpOptions) {
    if (!TaskID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TaskID].');
        return;
    }
    var url = parse('/edge/v1/proc/task/:TaskID/timings', [TaskID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.error.countTypeByEngine.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/error/count_type_by_engine');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.errors.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/errors');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.instances.byCategoryHost.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/instances/by_category_host');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.categories.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats/categories');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.engines.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats/engines');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.organizations.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats/organizations');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.timeRanges.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats/time_ranges');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.scheduledJobs.totalPendingTimeRanges.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/scheduled_jobs/total_pending_time_ranges');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.workrequest.detail.get = function(WorkRequestID, httpOptions) {
    if (!WorkRequestID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [WorkRequestID].');
        return;
    }
    var url = parse('/edge/v1/proc/workrequest/:WorkRequestID/detail', [WorkRequestID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.template.launch.post = function(TemplateID, httpOptions) {
    if (!TemplateID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [TemplateID].');
        return;
    }
    var url = parse('/edge/v1/proc/template/:TemplateID/launch', [TemplateID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.task.listreports.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/task/listreports');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.endpoint.post = function(Endpoint, httpOptions) {
    if (!Endpoint) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [Endpoint].');
        return;
    }
    var url = parse('/edge/v1/proc/endpoint/:Endpoint', [Endpoint]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proc.job.recheckStatus.get = function(JobID, httpOptions) {
    if (!JobID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [JobID].');
        return;
    }
    var url = parse('/edge/v1/proc/job/:JobID/recheck_status', [JobID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.stats.export.get = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/stats/export');
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

endpoint.processing.proc.tasks.updateStatus.post = function(httpOptions) {
    var url = parse('/edge/v1/proc/tasks/update_status');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proxy.getwork.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/proxy/:HostID/getwork', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proxy.register.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/proxy/:HostID/register', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proxy.terminate.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/proxy/:HostID/terminate', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.proxy.update.post = function(HostID, httpOptions) {
    if (!HostID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [HostID].');
        return;
    }
    var url = parse('/edge/v1/proxy/:HostID/update', [HostID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.security.getstorageserverpublickeyset.post = function(httpOptions) {
    var url = parse('/edge/v1/security/getstorageserverpublickeyset');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.service.instance.create.post = function(httpOptions) {
    var url = parse('/edge/v1/service/instance/create');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.service.instance.delete.post = function(ServiceInstanceID, httpOptions) {
    if (!ServiceInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceInstanceID].');
        return;
    }
    var url = parse('/edge/v1/service/instance/:ServiceInstanceID/delete', [ServiceInstanceID]);
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

endpoint.processing.service.instance.resources.get = function(ServiceInstanceID, httpOptions) {
    if (!ServiceInstanceID) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [ServiceInstanceID].');
        return;
    }
    var url = parse('/edge/v1/service/instance/:ServiceInstanceID/resources', [ServiceInstanceID]);
    sys.logs.debug('[veritoneuser] GET from: ' + url);
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options);
};

// Old Compatibles Helpers

endpoint.gql = {};

endpoint.gql = function(query, variables, httpOptions) {
    if (!query || !variables) {
        sys.logs.error('Invalid argument received. This helper should receive the following parameters as non-empty strings: [query,variables].');
        return;
    }
    var url = parse('/v3/graphql');
    sys.logs.debug('[veritoneuser] POST from: ' + url);
    httpOptions = httpOptions || {};
    httpOptions.query = query;
    httpOptions.variables = variables;
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options);
};

////////////////////////////////////
// Public API - Generic Functions //
////////////////////////////////////

endpoint.get = function(url, httpOptions, callbackData, callbacks) {
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._get(options, callbackData, callbacks);
};

endpoint.post = function(url, httpOptions, callbackData, callbacks) {
    options = checkHttpOptions(url, httpOptions);
    return endpoint._post(options, callbackData, callbacks);
};

endpoint.put = function(url, httpOptions, callbackData, callbacks) {
    options = checkHttpOptions(url, httpOptions);
    return endpoint._put(options, callbackData, callbacks);
};

endpoint.patch = function(url, httpOptions, callbackData, callbacks) {
    options = checkHttpOptions(url, httpOptions);
    return endpoint._patch(options, callbackData, callbacks);
};

endpoint.delete = function(url, httpOptions, callbackData, callbacks) {
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._delete(options, callbackData, callbacks);
};

endpoint.head = function(url, httpOptions, callbackData, callbacks) {
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._head(options, callbackData, callbacks);
};

endpoint.options = function(url, httpOptions, callbackData, callbacks) {
    var options = checkHttpOptions(url, httpOptions);
    return endpoint._options(options, callbackData, callbacks);
};

endpoint.utils = {};
            
endpoint.utils.parseTimestamp = function(dateString) {
    if (!dateString) {
        return null;
    }
    var dt = dateString.split(/[: T\-]/).map(parseFloat);
    return new Date(dt[0], dt[1] - 1, dt[2], dt[3] || 0, dt[4] || 0, dt[5] || 0, 0);
};

endpoint.utils.formatTimestamp = function(date) {
    if (!date) {
        return null;
    }
    var pad = function(number) {
        var r = String(number);
        if ( r.length === 1 ) {
            r = '0' + r;
        }
        return r;
    };
    return date.getUTCFullYear()
        + '-' + pad( date.getUTCMonth() + 1 )
        + '-' + pad( date.getUTCDate() )
        + 'T' + pad( date.getUTCHours() )
        + ':' + pad( date.getUTCMinutes() )
        + ':' + pad( date.getUTCSeconds() )
        + '.' + String( (date.getUTCMilliseconds()/1000).toFixed(3) ).slice( 2, 5 )
        + 'Z';
};

///////////////////////
//  Private helpers  //
///////////////////////

var mergeJSON = function (json1, json2) {
    const result = {};
    var key;
    for (key in json1) {
        if(json1.hasOwnProperty(key)) result[key] = json1[key];
    }
    for (key in json2) {
        if(json2.hasOwnProperty(key)) result[key] = json2[key];
    }
    return result;
}

var concatQuery = function (url, key, value) {
    return url + ((!url || url.indexOf('?') < 0) ? '?' : '&') + key + "=" + value;
};

var checkHttpOptions = function (url, options) {
    options = options || {};
    if (!!url) {
        if (isObject(url)) {
            // take the 'url' parameter as the options
            options = url || {};
        } else {
            if (!!options.path || !!options.params || !!options.body) {
                // options contains the http package format
                options.path = url;
            } else {
                // create html package
                options = {
                    path: url,
                    body: options
                }
            }
        }
    }
    return options;
};

var isObject = function (obj) {
    return !!obj && stringType(obj) === '[object Object]'
};

var stringType = Function.prototype.call.bind(Object.prototype.toString);

var parse = function (str) {
    try {
        if (arguments.length > 1) {
            var args = arguments[1], i = 0;
            return str.replace(/(:(?:\w|-)+)/g, () => {
                if (typeof (args[i]) != 'string') throw new Error('Invalid type of argument: [' + args[i] + '] for url [' + str + '].');
                return args[i++];
            });
        } else {
            if (str) {
                return str;
            }
            throw new Error('No arguments nor url were received when calling the helper. Please check it\'s definition.');
        }
    } catch (err) {
        sys.logs.error('Some unexpected error happened during the parse of the url for this helper.')
        throw err;
    }
}